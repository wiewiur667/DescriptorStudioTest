<UserControl xmlns:Views="clr-namespace:DescriptorStudioTest.Views"  xmlns:DescriptorStudioTest="clr-namespace:DescriptorStudioTest"
             x:Class="DescriptorStudioTest.Views.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:DescriptorStudioTest.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel,IsDesignTimeCreatable=True }"
             cal:Bind.AtDesignTime="True"
             MinHeight="300" MinWidth="300">
    <Grid Background="#FF007ACC">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="WindowName" Grid.Row="0"/>
        <ContentControl x:Name="MyTree" Grid.Row="1"/>
    </Grid>
</UserControl>
